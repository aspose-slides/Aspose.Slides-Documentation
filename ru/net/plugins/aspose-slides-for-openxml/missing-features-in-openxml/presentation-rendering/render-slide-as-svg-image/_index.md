---
title: Отобразить слайд как SVG‑изображение
type: docs
weight: 50
url: /ru/net/render-slide-as-svg-image/
---
SVG—это аббревиатура от Scalable Vector Graphics—это стандартный тип графики или формат, используемый для отображения двумерных изображений. SVG сохраняет изображения как векторы в XML с деталями, определяющими их поведение или внешний вид.

SVG является одним из немногих форматов изображений, отвечающих очень высоким требованиям в следующих аспектах: масштабируемость, интерактивность, производительность, доступность, программируемость и др. По этим причинам его часто используют в веб‑разработке.

You may want to use SVG files in these scenarios:

- когда вы планируете печатать свою презентацию в очень большом формате. Изображения SVG могут масштабироваться до любого разрешения или уровня. Вы можете изменять размер SVG‑изображений сколько угодно раз без потери качества.
- когда вы хотите использовать графики и диаграммы из своих слайдов в разных носителях или платформах. Большинство программ могут интерпретировать SVG‑файлы.
- когда вам нужны минимальные размеры изображений. SVG‑файлы обычно меньше своих аналогов высокого разрешения в других форматах, особенно в растровых (JPEG или PNG).

Aspose.Slides for .NET позволяет экспортировать слайды ваших презентаций в виде изображений **SVG**. Чтобы создать SVG‑изображение из любого слайда, выполните следующее:

- Создайте экземпляр класса Presentation.
- Пройдитесь по всем слайдам в презентации.
- Запишите каждый слайд в отдельный SVG‑файл с помощью FileStream.

{{% alert color="info" %}}
Возможно, вам захочется попробовать наше [бесплатное веб‑приложение](https://products.aspose.app/slides/ru/conversion/ppt-to-svg), в котором реализована функция преобразования PPT в SVG от Aspose.Slides for .NET.
{{% /alert %}}

Этот пример кода на C# показывает, как преобразовать PPT в SVG с помощью Aspose.Slides:

``` csharp
using Aspose.Slides;

using (Presentation pres = new Presentation("pres.pptx"))
{
    for (var index = 0; index < pres.Slides.Count; index++)
    {
        ISlide slide = pres.Slides[index];

        using (FileStream fileStream = new FileStream($"slide-{index}.svg", FileMode.Create, FileAccess.Write))
        {
            slide.WriteAsSvg(fileStream);   
        }
    }
}
```