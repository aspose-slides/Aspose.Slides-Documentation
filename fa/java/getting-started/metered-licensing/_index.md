---
title: مجوز متری
type: docs
weight: 100
url: /fa/java/metered-licensing/
keywords:
- مجوز
- مجوز متری
- کلیدهای مجوز
- کلید عمومی
- کلید خصوصی
- مقدار مصرف
- PowerPoint
- OpenDocument
- ارائه
- Java
- Aspose.Slides
description: "بیاموزید چگونه مجوز متری Aspose.Slides برای جاوا به شما امکان می‌دهد فایل‌های PowerPoint و OpenDocument را به‌صورت انعطاف‌پذیر پردازش کنید و فقط برای استفاده‌تان هزینه پرداخت کنید."
---
## **معرفی**

مجوز متری روشی برای مجوزدهی است که می‌تواند همراه با روش‌های مجوزدهی موجود استفاده شود. اگر می‌خواهید براساس استفاده‌تان از قابلیت‌های API Aspose.Slides هزینه‌گیری شوید، مجوز متری را انتخاب می‌کنید.

## **اعمال کلیدهای متری**

{{% alert color="primary" %}} 

مجوز متری یک مکانیزم جدید برای مجوزدهی است که می‌تواند همراه با روش‌های مجوزدهی موجود استفاده شود. اگر می‌خواهید براساس استفاده‌تان از قابلیت‌های API Aspose.Slides هزینه‌گیری شوید، مجوز متری را انتخاب می‌کنید.

هنگامی که یک مجوز متری خریداری می‌کنید، کلیدها (و نه فایل مجوز) دریافت می‌کنید. این کلید متری می‌تواند با استفاده از کلاس [Metered](https://reference.aspose.com/slides/fa/java/com.aspose.slides/metered/) که Aspose برای عملیات‌ متری فراهم کرده است، اعمال شود. برای جزئیات بیشتر، به [پرسش‌های متداول مجوز متری](https://purchase.aspose.com/faqs/licensing/metered) مراجعه کنید.

{{% /alert %}} 

1. یک نمونه از کلاس [Metered](https://reference.aspose.com/slides/fa/java/com.aspose.slides/metered/) ایجاد کنید.

1. کلیدهای عمومی و خصوصی خود را به متد [setMeteredKey](https://reference.aspose.com/slides/fa/java/com.aspose.slides/metered/#setMeteredKey-java.lang.String-java.lang.String-) پاس بدهید.

1. مقداری پردازش انجام دهید (کارها را اجرا کنید).

1. متد [getConsumptionQuantity](https://reference.aspose.com/slides/fa/java/com.aspose.slides/metered/#getConsumptionQuantity--) کلاس `Metered` را فراخوانی کنید.

شما باید مقدار/تعداد درخواست‌های API که تا کنون مصرف کرده‌اید را ببینید.

این کد نمونه نشان می‌دهد چگونه از مجوز متری استفاده کنید:

```java
// یک نمونه از کلاس Metered ایجاد می‌کند
com.aspose.slides.Metered metered = new com.aspose.slides.Metered();

try {
    // کلیدهای عمومی و خصوصی را به شی Metered پاس می‌دهد
    metered.setMeteredKey("<valid public key>", "<valid private key>");

    // مقدار مصرف شده را قبل از فراخوانی API دریافت می‌کند
    double amountBefore = com.aspose.slides.Metered.getConsumptionQuantity();
    System.out.println("Amount consumed before: " + amountBefore);

    // در اینجا کاری با API Aspose.Slides انجام دهید
    // ...

    // مقدار مصرف شده را پس از فراخوانی API دریافت می‌کند
    double amountAfter = com.aspose.slides.Metered.getConsumptionQuantity();
    System.out.println("Amount consumed after: " + amountAfter);
} catch (Exception ex) {
    ex.printStackTrace();
}
```

{{% alert color="warning" title="NOTE"  %}} 

برای استفاده از مجوز متری، به یک اتصال اینترنتی پایدار نیاز دارید چون این مکانیزم مجوزدهی برای تعامل مداوم با سرویس‌های ما و انجام محاسبات از اینترنت استفاده می‌کند.

{{% /alert %}} 

## **پرسش‌های متداول**

**آیا می‌توانم یک مجوز متری را همراه با یک مجوز معمولی (دائم یا موقت) در یک برنامه استفاده کنم؟**

بله. مجوز متری یک مکانیزم مجوزدهی اضافی است که می‌تواند همراه با [روش‌های مجوزدهی](/slides/fa/java/licensing/) موجود استفاده شود. شما هنگام شروع برنامه تصمیم می‌گیرید از کدام مکانیزم استفاده کنید.

**دقیقاً چه چیزی تحت یک مجوز متری به‌عنوان مصرف محسوب می‌شود: عملیات‌ها یا فایل‌ها؟**

مصرف بر پایه استفاده از API شمارش می‌شود، یعنی تعداد درخواست‌ها یا عملیات‌ها. می‌توانید مصرف فعلی را از طریق [روش‌های ردیابی مصرف](https://reference.aspose.com/slides/fa/java/com.aspose.slides/metered/) دریافت کنید.

**آیا مجوز متری برای میکروسرویس‌ها و محیط‌های بدون سرور که نمونه‌ها به‌طور مکرر راه‌اندازی مجدد می‌شوند مناسب است؟**

بله. از آنجا که حساب‌گذاری در سطح تماس API انجام می‌شود، سناریوهای دارای راه‌اندازی سرد مکرر سازگار هستند، به‌شرط داشتن دسترسی شبکه پایدار برای محاسبات متری.

**آیا عملکرد کتابخانه هنگام استفاده از مجوز متری نسبت به مجوز دائم متفاوت است؟**

خیر. این فقط در مورد مکانیزم مجوزدهی و صورتحساب است؛ قابلیت‌های محصول یکسان هستند.

**مجوز متری چگونه با نسخه آزمایشی و مجوز موقت مرتبط است؟**

نسخه آزمایشی محدودیت‌ها و واترمارک دارد، [مجوز موقت](https://purchase.aspose.com/temporary-license/) محدودیت‌ها را به مدت 30 روز حذف می‌کند و مجوز متری محدودیت‌ها را حذف کرده و بر اساس استفاده واقعی هزینه‌گیری می‌کند.

**آیا می‌توانم با واکنش خودکار هنگام عبور از آستانه مصرف، بودجه را کنترل کنم؟**

بله. یک روش معمول این است که به‌صورت دوره‌ای مصرف فعلی را از طریق [روش‌های ردیابی] دریافت کنید و محدودیت‌ها یا هشدارهای خود را در سطح برنامه یا نظارت پیاده‌سازی کنید.