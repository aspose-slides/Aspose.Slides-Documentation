---
title: سفارشی‌سازی نوارهای خطا در نمودارهای ارائه با استفاده از С++
linktitle: نوار خطا
type: docs
url: /fa/cpp/error-bar/
keywords:
- نوار خطا
- مقدار سفارشی
- PowerPoint
- ارائه
- С++
- Aspose.Slides
description: "یاد بگیرید چگونه نوارهای خطا را در نمودارها با Aspose.Slides برای С++ اضافه و سفارشی کنید — نمایش داده‌ها را در ارائه‌های PowerPoint بهینه کنید."
---
## **بررسی کلی**

این مقاله توضیح می‌دهد که چگونه با استفاده از Aspose.Slides با نوارهای خطا در نمودارهای ارائه کار کنید. نشان می‌دهد چگونه نوارهای خطا را به یک سری نمودار اضافه کنید، تنظیمات نوارهای خطای X و Y را پیکربندی کنید و انواع مقدار مختلفی مانند ثابت، درصدی و سفارشی را اعمال کنید.

همچنین نحوه اختصاص مقادیر نوار خطای سفارشی برای نقاط دادهٔ فردی در یک سری را با استفاده از مجموعهٔ نقاط دادهٔ مربوطه نشان می‌دهد. علاوه بر این، مقاله نکات مختصری دربارهٔ رفتار نوارهای خطا هنگام صادرات، سازگاری آن‌ها با مارکرها و برچسب‌های داده و مکان یافتن کلاس‌ها و enumهای مرتبط در مرجع API ارائه می‌دهد.

## **افزودن نوارهای خطا**
Aspose.Slides برای C++ یک API ساده برای مدیریت مقادیر نوار خطا فراهم می‌کند. کد نمونه زمانی اعمال می‌شود که از نوع مقدار سفارشی استفاده می‌شود. برای تعیین یک مقدار، از ویژگی **ErrorBarCustomValues** یک نقطه دادهٔ خاص در مجموعهٔ **DataPoints** سری استفاده کنید:

1. یک نمونه از کلاس [Presentation](https://reference.aspose.com/slides/fa/cpp/aspose.slides/presentation/) ایجاد کنید.
1. یک نمودار حبابی در اسلاید موردنظر اضافه کنید.
1. به اولین سری نمودار دسترسی پیدا کنید و قالب نوار خطای X را تنظیم کنید.
1. به اولین سری نمودار دسترسی پیدا کنید و قالب نوار خطای Y را تنظیم کنید.
1. تنظیم مقادیر و قالب نوارها.
1. ارائهٔ تغییر یافته را در یک فایل PPTX ذخیره کنید.

{{< gist "aspose-slides" "a690df625dc0b1fff869ab198affe7a4" "Examples-SlidesCPP-AddErrorBars-AddErrorBars.cpp" >}}

## **افزودن نوارهای خطای سفارشی**
Aspose.Slides برای C++ یک API ساده برای مدیریت مقادیر نوار خطای سفارشی فراهم می‌کند. کد نمونه زمانی اعمال می‌شود که ویژگی **IErrorBarsFormat.ValueType** برابر با **Custom** باشد. برای تعیین یک مقدار، از ویژگی **ErrorBarCustomValues** یک نقطه دادهٔ خاص در مجموعهٔ **DataPoints** سری استفاده کنید:

1. یک نمونه از کلاس [Presentation](https://reference.aspose.com/slides/fa/cpp/aspose.slides/presentation/) ایجاد کنید.
1. یک نمودار حبابی در اسلاید موردنظر اضافه کنید.
1. به اولین سری نمودار دسترسی پیدا کنید و قالب نوار خطای X را تنظیم کنید.
1. به اولین سری نمودار دسترسی پیدا کنید و قالب نوار خطای Y را تنظیم کنید.
1. به نقاط دادهٔ فردی سری نمودار دسترسی پیدا کنید و مقادیر نوار خطا را برای یک نقطه دادهٔ فردی تنظیم کنید.
1. تنظیم مقادیر و قالب نوارها.
1. ارائهٔ تغییر یافته را در یک فایل PPTX ذخیره کنید.

{{< gist "aspose-slides" "a690df625dc0b1fff869ab198affe7a4" "Examples-SlidesCPP-AddCustomError-AddCustomError.cpp" >}}

## **پرسش‌های متداول**

**هنگام صادر کردن ارائه به PDF یا تصویر، نوارهای خطا چه می‌شوند؟**

آن‌ها به عنوان بخشی از نمودار رندر می‌شوند و در طول تبدیل همراه با بقیهٔ قالب‌بندی نمودار حفظ می‌گردند، مشروط بر اینکه نسخه یا رندرری سازگار باشد.

**آیا می‌توان نوارهای خطا را با مارکرها و برچسب‌های داده ترکیب کرد؟**

بله. نوارهای خطا یک عنصر جداگانه هستند و با مارکرها و برچسب‌های داده سازگارند؛ اگر عناصر روی هم پوشانی داشته باشند، ممکن است نیاز به تنظیم قالب‌بندی داشته باشید.

**کجا می‌توان فهرست ویژگی‌ها و enum‌های مربوط به نوارهای خطا را در API یافت؟**

در مرجع API: کلاس [ErrorBarsFormat](https://reference.aspose.com/slides/fa/cpp/aspose.slides.charts/errorbarsformat/) و enumهای مرتبط [ErrorBarType](https://reference.aspose.com/slides/fa/cpp/aspose.slides.charts/errorbartype/) و [ErrorBarValueType](https://reference.aspose.com/slides/fa/cpp/aspose.slides.charts/errorbarvaluetype/).