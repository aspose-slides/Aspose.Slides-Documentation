# Node.js via Java validation

Compilation is not applicable. Run `tools/nodejs-java/snippet-check/check-snippets.ps1` with `-Docker`
to perform syntax checking and execution against the real `aspose.slides.via.java` package.
`node --check` alone is insufficient.

Report `compilation: not applicable`, then the syntax and API runtime results separately.
